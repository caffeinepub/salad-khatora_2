{
  "frontend/src/index.css": "Global styles with OKLCH color system using white background and green primary theme; includes custom Inter/Poppins fonts, light/dark mode tokens, and print media query for bill receipts.",
  "frontend/tailwind.config.js": "Extended Tailwind config with white/green color palette using OKLCH tokens, Poppins/Inter font families, custom border radii, and green glow shadow utilities.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file with all localStorage-based hooks (menu items, combos, suppliers, ingredients, purchase orders, waste logs, alerts, subscriptions) and all backend-integrated hooks (customers, loyalty, discount codes, tax configs, sale orders, staff accounts, audit logs, admin); `useCreateCustomer` and `useUpdateCustomer` now throw a descriptive error when the actor is unavailable, and `useSalesReport` accepts an optional `period` parameter and returns `{ data, isLoading, isError, error }`.",
  "frontend/src/App.tsx": "TanStack Router setup with all user-facing routes under a shared Layout wrapper; login route has no layout, index redirects to dashboard, all pages are wired correctly.",
  "frontend/src/components/Layout.tsx": "Responsive app shell with grouped sidebar navigation (Overview, Sales & Orders, Catalog, Operations sections); active nav items highlighted in green, mobile hamburger menu, dark mode toggle, and logout button using useInternetIdentity.",
  "frontend/src/pages/LoginPage.tsx": "Login page with green/white theme, Internet Identity sign-in button using the `isLoggingIn` and `isLoginError` helper states (instead of direct `loginStatus` string comparison), and auto-redirect to /dashboard after successful authentication.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page showing total revenue, orders, and average order value stats computed from sale orders, plus recent orders list and quick navigation links.",
  "frontend/src/pages/InventoryPage.tsx": "Full inventory management page with sortable table including new Supplier and Expiry Date columns (expiry dates within 7 days highlighted in red), add/edit dialogs with supplier selector and expiry date picker, mobile card layout, and delete confirmation.",
  "frontend/src/components/AddIngredientForm.tsx": "Form for adding a new ingredient using the new `useCreateIngredient` hook and `CreateIngredientRequest` type, with all fields including optional supplier selector (from `useSuppliers`) and expiry date picker.",
  "frontend/src/components/EditIngredientForm.tsx": "Pre-populated edit form for an existing `Ingredient` using `useUpdateIngredient` and `UpdateIngredientRequest` types, with supplier selector and expiry date picker pre-filled from existing values.",
  "frontend/src/components/ProfileSetupModal.tsx": "Profile setup modal that accepts `onSave` and `isSaving` props instead of managing its own mutation, allowing the parent to control the save logic.",
  "frontend/src/lib/utils.ts": "Utility function `cn` combining clsx and tailwind-merge for conditional class name composition.",
  "frontend/src/pages/MenuPage.tsx": "Menu management page with add/edit/delete/toggle dialogs; fixed `costPerServing` to be properly optional with null-safe access, removed incompatible `ingredients`/`availableFromHour`/`availableToHour`/`availableDays` fields from the form to match the updated `MenuItem` type.",
  "frontend/src/pages/SalesPage.tsx": "Point-of-sale page fixed to correctly cast `itemId` and `quantity` to `bigint` for `SaleOrderItem`, and pass `discountCodeId`/`customerId` as `bigint | null` to `useCreateSaleOrder`.",
  "frontend/src/pages/CustomersPage.tsx": "Customer management page with Add/Edit/Delete dialogs and a detail view; the Add Customer button and submit button are disabled while the actor is initializing, inline alerts show backend connection status and mutation errors, and the detail view is rendered by replacing the list view (using `onBack` prop on `CustomerDetailView`) instead of a modal.",
  "frontend/src/components/CustomerDetailView.tsx": "Customer detail component showing profile, loyalty points with redeem dialog, and order history; fixed to use `useCustomers` + find instead of non-existent `useCustomer`, uses `useRedeemLoyaltyPoints` hook with proper `bigint` types, and passes `customerId` (bigint) directly to loyalty/order hooks.",
  "frontend/src/pages/SubscriptionsPage.tsx": "Subscriptions page fixed to safely access `sub.menuItemIds` with a fallback to an empty array (`sub.menuItemIds ?? []`) to avoid the \"possibly undefined\" TypeScript error.",
  "frontend/src/pages/AlertsPage.tsx": "Alerts page displaying all alerts with type-specific icons and badges; fixed `markAllRead.mutate()` to pass the array of unread alert IDs as required by the `useMarkAllAlertsRead` mutation signature.",
  "frontend/src/pages/SuppliersPage.tsx": "Suppliers page updated to use `contactName` (matching the `Supplier` interface) instead of `contactPerson`, removed the broken `useAutoGeneratePurchaseOrders` import and usage.",
  "frontend/src/pages/PurchaseOrdersPage.tsx": "Purchase orders page rewritten to use the correct `PurchaseOrderStatus` type values (`pending`/`confirmed`/`delivered`/`cancelled`), `costPerUnit` instead of `estimatedCost` in items, `expectedDelivery` as a number timestamp, and removed the broken `useAutoGeneratePurchaseOrders` call.",
  "frontend/src/pages/WasteLogPage.tsx": "Waste log page fixed to use `useWasteStats` as a plain object (not a query), use `costPerUnit` instead of `costPrice` for cost estimation, and use `createdAt` instead of `loggedAt` for sorting and display.",
  "frontend/src/pages/CombosPage.tsx": "Combo Deals management page with card grid layout, add/edit dialog with menu item multi-selector and live price calculation, inline toggle/delete/edit actions; fixed `updateCombo.mutateAsync` call to use `id` instead of `comboId` to match the hook's expected parameter shape.",
  "frontend/src/pages/AdminSettingsPage.tsx": "Admin settings page with three clear UI states — (1) current user is admin: shows reassign and vacate controls, (2) current user is not admin and loading is complete: always shows a \"Claim Admin Role\" card with explanatory text and a claim button (backend rejects if already assigned), (3) loading: shows spinner. Removed the broken `isAdminVacant` / \"Info for non-admin\" logic that was preventing the Claim button from appearing.",
  "frontend/src/pages/SalesReportsPage.tsx": "Sales reports page that accepts a `period` parameter in `useSalesReport` and destructures `{ data, isLoading, isError, error }` from the hook result, displaying daily or weekly aggregated revenue and order counts in a summary card grid and sortable table.",
  "frontend/src/hooks/useDarkMode.ts": "Custom hook that manages dark mode state with localStorage persistence, applying/removing the 'dark' class on the HTML element and exposing isDark and toggle.",
  "frontend/src/pages/StaffAccountsPage.tsx": "Staff accounts management page fixed to parse the principal string into a `Principal` object using `Principal.fromText()` before passing to `useCreateStaffAccount`.",
  "frontend/src/pages/AuditLogPage.tsx": "Activity log page displaying paginated audit entries (20 per page) with timestamp, actor, action, targetType, targetId, and details columns, with a Load More button — admin-only redirect check removed.",
  "frontend/src/pages/TaxConfigPage.tsx": "Tax configuration management page with create/edit/toggle/delete operations; removed all authentication and identity checks (useInternetIdentity, useNavigate redirect) so any user can manage tax configs without login.",
  "frontend/src/pages/DiscountsPage.tsx": "Discount codes management page with full CRUD operations (create, edit, toggle, delete) available to all users without any admin role checks or login gates. Displays discount codes in a card grid with action buttons always visible, and includes Add/Edit dialogs with a shared DiscountForm component.",
  "frontend/src/components/CheckoutDialog.tsx": "New checkout dialog component that displays the full order summary (items, totals, discount, customer, note) and a required payment mode selector (Cash, Card, UPI/Online) with radio-style buttons; the Confirm Order button is disabled until a payment mode is selected and shows a spinner while processing.",
  "frontend/src/components/BillReceiptView.tsx": "New bill receipt dialog shown after a successful order, displaying order ID, date/time, itemized list with quantities and unit prices, discount, tax breakdown, grand total, and payment mode; includes a Print Bill button (calls window.print()) and a New Sale button that triggers cart cleanup.",
  "frontend/src/pages/DeliveryCalendarPage.tsx": "Delivery calendar page rewritten to work with `nextRenewalDate` as a `number` timestamp; `isWithinNextDays` accepts `number`, date grouping uses `toISOString().split(\"T\")[0]` on a `Date` object constructed from the timestamp."
}