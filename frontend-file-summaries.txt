{
  "frontend/src/index.css": "Global styles with a fresh white-and-green OKLCH color system, custom font imports (Plus Jakarta Sans + DM Sans), and design tokens for light/dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom green primary palette, heading/body font families, additional border radii, and green glow shadow utilities.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend and local operations; added missing hooks (`useCustomerOrderHistory`, `useLoyaltyBalance`, `useRedeemLoyaltyPoints`, `useUpcomingDeliveries`, `usePauseSubscription`, `useResumeSubscription`, `useCancelSubscription`), added backward-compatibility aliases for combo hooks (`useCreateComboDeal`, `useUpdateComboDeal`, `useDeleteComboDeal`, `useToggleComboDealAvailability`), simplified `useCreateWasteLog` to accept only `{ingredientId, quantity, reason}` and auto-derive the rest from the ingredients store, and fixed localStorage-based user profile hooks.",
  "frontend/src/App.tsx": "TanStack Router setup with all routes including /staff and /audit-log, wrapped in QueryClientProvider, with a named IndexRedirect component (fixing the hooks-in-inline-component lint error) and theme restoration on mount.",
  "frontend/src/components/Layout.tsx": "Responsive app shell with sidebar navigation including a new Admin section (Staff Accounts, Activity Log, Admin Settings links), dark mode toggle button (sun/moon) in the header using useDarkMode hook, and branded footer.",
  "frontend/src/pages/LoginPage.tsx": "Login page with Internet Identity sign-in button, brand logo, feature highlights, loading/error states, and auto-redirect if already authenticated.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page showing sales stats and inventory summary cards; simplified `handleSaveProfile` to remove the redundant manual `invalidateQueries` call (now handled inside the mutation's `onSuccess`).",
  "frontend/src/pages/InventoryPage.tsx": "Full inventory management page with sortable table including new Supplier and Expiry Date columns (expiry dates within 7 days highlighted in red), add/edit dialogs with supplier selector and expiry date picker, mobile card layout, and delete confirmation.",
  "frontend/src/components/AddIngredientForm.tsx": "Form for adding a new ingredient using the new `useCreateIngredient` hook and `CreateIngredientRequest` type, with all fields including optional supplier selector (from `useSuppliers`) and expiry date picker.",
  "frontend/src/components/EditIngredientForm.tsx": "Pre-populated edit form for an existing `Ingredient` using `useUpdateIngredient` and `UpdateIngredientRequest` types, with supplier selector and expiry date picker pre-filled from existing values.",
  "frontend/src/components/ProfileSetupModal.tsx": "Profile setup modal that accepts `onSave` and `isSaving` props instead of managing its own mutation, allowing the parent to control the save logic.",
  "frontend/src/lib/utils.ts": "Utility function `cn` combining clsx and tailwind-merge for conditional class name composition.",
  "frontend/src/pages/MenuPage.tsx": "Menu management page extended with profit margin badges (green/red TrendingUp/Down icons with percentage) on each card, expandable cost breakdown section showing cost per serving/selling price/gross profit/margin, and time-based availability fields in the Add/Edit dialog (from/to hour inputs and day-of-week checkboxes), with schedule summary badge displayed on cards when constraints are set.",
  "frontend/src/pages/SalesPage.tsx": "Billing and sales page; fixed `createOrder.mutateAsync` to pass `itemId` and `quantity` as `number` (not `bigint`) matching the hook's parameter type, and changed `null` to `undefined` for optional `discountCodeId` and `customerId` fields.",
  "frontend/src/pages/CustomersPage.tsx": "Customer management page with searchable table (name/email), add/edit dialogs with validation (name and email required), delete confirmation, and row-click navigation to CustomerDetailView; uses useCustomers, useCreateCustomer, useUpdateCustomer, useDeleteCustomer hooks.",
  "frontend/src/components/CustomerDetailView.tsx": "Customer detail component showing full profile (name, email, phone, address, notes, joined date) and a list of linked subscriptions with status badges, renewal dates, and pricing; uses useCustomer and useSubscriptions hooks with a back button to return to the customers list.",
  "frontend/src/pages/SubscriptionsPage.tsx": "Subscription management page with create/pause/resume/cancel actions, now correctly passing `status` and `nextRenewalDate` fields when creating subscriptions.",
  "frontend/src/pages/AlertsPage.tsx": "Alerts page displaying all alerts with type-specific icons and badges including new orange expiry warning alerts (CalendarX icon), mark-as-read per alert and mark-all-as-read functionality, and unread count badge.",
  "frontend/src/pages/SuppliersPage.tsx": "Supplier management page with CRUD operations, now correctly passing `createdAt` from the original supplier when updating to satisfy the full `Supplier` type.",
  "frontend/src/pages/PurchaseOrdersPage.tsx": "Purchase orders page importing `PurchaseOrderStatus` from `useQueries` (not backend), using `number` IDs for status updates and order display.",
  "frontend/src/pages/WasteLogPage.tsx": "Waste log page for recording and viewing ingredient waste; updated `createWasteLog.mutateAsync` call to only pass `{ingredientId, quantity, reason}` matching the simplified hook signature that auto-derives `ingredientName`, `unit`, and `costLoss`.",
  "frontend/src/pages/CombosPage.tsx": "Combo Deals management page with card grid layout, add/edit dialog with menu item multi-selector and live price calculation, inline toggle/delete/edit actions; fixed `updateCombo.mutateAsync` call to use `id` instead of `comboId` to match the hook's expected parameter shape.",
  "frontend/src/pages/AdminSettingsPage.tsx": "Admin settings page with three clear UI states — (1) current user is admin: shows reassign and vacate controls, (2) current user is not admin and loading is complete: always shows a \"Claim Admin Role\" card with explanatory text and a claim button (backend rejects if already assigned), (3) loading: shows spinner. Removed the broken `isAdminVacant` / \"Info for non-admin\" logic that was preventing the Claim button from appearing.",
  "frontend/src/pages/SalesReportsPage.tsx": "Sales reports page with daily/weekly period selector and summary stats; fixed all `row.date` references to `row.period` to match the `SalesReportEntry` interface, and added explicit `SalesReportEntry` type import.",
  "frontend/src/hooks/useDarkMode.ts": "Custom hook that manages dark mode state with localStorage persistence, applying/removing the 'dark' class on the HTML element and exposing isDark and toggle.",
  "frontend/src/pages/StaffAccountsPage.tsx": "Admin-only staff accounts management page with a table showing name, truncated principal (with tooltip), role badge, status badge, and created date, plus add/edit/delete dialogs using the staff account hooks.",
  "frontend/src/pages/AuditLogPage.tsx": "Admin-only activity log page displaying paginated audit entries (20 per page) with timestamp, actor, action, targetType, targetId, and details columns, with a Load More button for pagination.",
  "frontend/src/pages/TaxConfigPage.tsx": "Tax configuration management page with add/edit/delete/toggle, redirecting to `/dashboard` instead of the non-existent `/login` route when unauthenticated."
}