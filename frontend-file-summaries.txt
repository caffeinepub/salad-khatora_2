{
  "frontend/src/index.css": "Global styles with OKLCH color system and custom fonts; added `@media print` rules that hide all app UI except the `#bill-receipt` element and the `.no-print` class, with proper page size settings for receipt printing.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom green primary palette, heading/body font families, additional border radii, and green glow shadow utilities.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file with all backend-integrated and localStorage hooks; added missing hooks `useToggleMenuItemAvailability`, `useToggleComboDealAvailability`, `useCreateComboDeal`/`useUpdateComboDeal`/`useDeleteComboDeal` aliases, `usePauseSubscription`, `useResumeSubscription`, `useCancelSubscription`, `useAutoGeneratePurchaseOrders`, and `paymentType` support in `useCreateSaleOrder`.",
  "frontend/src/App.tsx": "TanStack Router setup with all user-facing routes; removed LoginPage, ProtectedLayout auth guard, StaffAccountsPage, AuditLogPage, and AdminSettingsPage routes — app now opens directly to the dashboard without any authentication requirement. Added SuppliersPage route back.",
  "frontend/src/components/Layout.tsx": "Responsive app shell with grouped sidebar navigation (Overview, Sales & Orders, Catalog, Operations sections); removed all admin nav items (Staff Accounts, Activity Log), logout button, identity display, and useInternetIdentity import for a clean user-friendly experience.",
  "frontend/src/pages/LoginPage.tsx": "Login page with general \"Welcome Back\" heading (replacing \"Admin Login\"), Internet Identity sign-in button, loading/error states, and auto-redirect to `/dashboard` for any authenticated user.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page showing sales stats and inventory summary cards; fixed field references from non-existent `todayRevenue`/`todayOrders`/`weekRevenue`/`weekOrders` to the correct `totalRevenue`/`totalOrders`/`avgOrderValue` fields returned by `useSalesStats`.",
  "frontend/src/pages/InventoryPage.tsx": "Full inventory management page with sortable table including new Supplier and Expiry Date columns (expiry dates within 7 days highlighted in red), add/edit dialogs with supplier selector and expiry date picker, mobile card layout, and delete confirmation.",
  "frontend/src/components/AddIngredientForm.tsx": "Form for adding a new ingredient using the new `useCreateIngredient` hook and `CreateIngredientRequest` type, with all fields including optional supplier selector (from `useSuppliers`) and expiry date picker.",
  "frontend/src/components/EditIngredientForm.tsx": "Pre-populated edit form for an existing `Ingredient` using `useUpdateIngredient` and `UpdateIngredientRequest` types, with supplier selector and expiry date picker pre-filled from existing values.",
  "frontend/src/components/ProfileSetupModal.tsx": "Profile setup modal that accepts `onSave` and `isSaving` props instead of managing its own mutation, allowing the parent to control the save logic.",
  "frontend/src/lib/utils.ts": "Utility function `cn` combining clsx and tailwind-merge for conditional class name composition.",
  "frontend/src/pages/MenuPage.tsx": "Menu management page extended with profit margin badges (green/red TrendingUp/Down icons with percentage) on each card, expandable cost breakdown section showing cost per serving/selling price/gross profit/margin, and time-based availability fields in the Add/Edit dialog (from/to hour inputs and day-of-week checkboxes), with schedule summary badge displayed on cards when constraints are set.",
  "frontend/src/pages/SalesPage.tsx": "Refactored POS page with a two-step checkout flow: clicking Checkout opens the CheckoutDialog (cart is NOT cleared), and only after successful backend confirmation does the BillReceiptView appear; cart and all fields are reset only when the user clicks \"New Sale\" on the bill.",
  "frontend/src/pages/CustomersPage.tsx": "Customer management page with Add/Edit/Delete dialogs; added `createCustomer.reset()` and `updateCustomer.reset()` calls when opening/closing dialogs to clear stale mutation error states, extracted `openAddDialog`/`closeAddDialog`/`openEdit`/`closeEditDialog` helpers for cleaner state management, and improved error message extraction to strip verbose ICP reject text prefixes.",
  "frontend/src/components/CustomerDetailView.tsx": "Customer detail component showing profile, loyalty points with redeem dialog, and order history; fixed to use `useCustomers` + find instead of non-existent `useCustomer`, uses `useRedeemLoyaltyPoints` hook with proper `bigint` types, and passes `customerId` (bigint) directly to loyalty/order hooks.",
  "frontend/src/pages/SubscriptionsPage.tsx": "Subscription management page with create/pause/resume/cancel actions, now correctly passing `status` and `nextRenewalDate` fields when creating subscriptions.",
  "frontend/src/pages/AlertsPage.tsx": "Alerts page displaying all alerts with type-specific icons and badges; fixed `markAllRead.mutate()` to pass the array of unread alert IDs as required by the `useMarkAllAlertsRead` mutation signature.",
  "frontend/src/pages/SuppliersPage.tsx": "Supplier management page with CRUD operations, now correctly passing `createdAt` from the original supplier when updating to satisfy the full `Supplier` type.",
  "frontend/src/pages/PurchaseOrdersPage.tsx": "Purchase orders page importing `PurchaseOrderStatus` from `useQueries` (not backend), using `number` IDs for status updates and order display.",
  "frontend/src/pages/WasteLogPage.tsx": "Waste log page for recording and viewing ingredient waste; updated `createWasteLog.mutateAsync` call to only pass `{ingredientId, quantity, reason}` matching the simplified hook signature that auto-derives `ingredientName`, `unit`, and `costLoss`.",
  "frontend/src/pages/CombosPage.tsx": "Combo Deals management page with card grid layout, add/edit dialog with menu item multi-selector and live price calculation, inline toggle/delete/edit actions; fixed `updateCombo.mutateAsync` call to use `id` instead of `comboId` to match the hook's expected parameter shape.",
  "frontend/src/pages/AdminSettingsPage.tsx": "Admin settings page with three clear UI states — (1) current user is admin: shows reassign and vacate controls, (2) current user is not admin and loading is complete: always shows a \"Claim Admin Role\" card with explanatory text and a claim button (backend rejects if already assigned), (3) loading: shows spinner. Removed the broken `isAdminVacant` / \"Info for non-admin\" logic that was preventing the Claim button from appearing.",
  "frontend/src/pages/SalesReportsPage.tsx": "Sales reports page with daily/weekly period selector and summary stats; fixed table row key and cell content from non-existent `row.period` to the correct `row.date` field from `SalesReportEntry`.",
  "frontend/src/hooks/useDarkMode.ts": "Custom hook that manages dark mode state with localStorage persistence, applying/removing the 'dark' class on the HTML element and exposing isDark and toggle.",
  "frontend/src/pages/StaffAccountsPage.tsx": "Staff accounts management page with table showing name, truncated principal (with tooltip), role badge, status badge, and created date, plus add/edit/delete dialogs — admin-only redirect check removed.",
  "frontend/src/pages/AuditLogPage.tsx": "Activity log page displaying paginated audit entries (20 per page) with timestamp, actor, action, targetType, targetId, and details columns, with a Load More button — admin-only redirect check removed.",
  "frontend/src/pages/TaxConfigPage.tsx": "Tax configuration management page with create/edit/toggle/delete operations; removed all authentication and identity checks (useInternetIdentity, useNavigate redirect) so any user can manage tax configs without login.",
  "frontend/src/pages/DiscountsPage.tsx": "Discount codes management page with full CRUD operations (create, edit, toggle, delete) available to all users without any admin role checks or login gates. Displays discount codes in a card grid with action buttons always visible, and includes Add/Edit dialogs with a shared DiscountForm component.",
  "frontend/src/components/CheckoutDialog.tsx": "New checkout dialog component that displays the full order summary (items, totals, discount, customer, note) and a required payment mode selector (Cash, Card, UPI/Online) with radio-style buttons; the Confirm Order button is disabled until a payment mode is selected and shows a spinner while processing.",
  "frontend/src/components/BillReceiptView.tsx": "New bill receipt dialog shown after a successful order, displaying order ID, date/time, itemized list with quantities and unit prices, discount, tax breakdown, grand total, and payment mode; includes a Print Bill button (calls window.print()) and a New Sale button that triggers cart cleanup."
}